import SwiftUI

// Skeleton for BACKLOG.md Epic L (analytics). No networking yet — see I2/L1.
struct AnalyticsView: View {
    var body: some View {
        NavigationStack {
            Text("Analytics")
                .navigationTitle("Analytics")
        }
    }
}

#Preview {
    AnalyticsView()
}
